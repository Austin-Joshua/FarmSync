{
  "name": "farmsync",
  "version": "1.0.0",
  "private": true,
  "description": "FarmSync - Modern Digital Farm Record Management System with Spring Boot and React/Zustand.",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend"
  }
}
