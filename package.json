{
  "name": "farmsync",
  "version": "1.0.0",
  "private": true,
  "description": "FarmSync - Digital Farm Record Management. Frontend-only (no backend); ML and datasets in project root.",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:frontend": "npm run dev --prefix client",
    "install:all": "npm install && npm install --prefix client"
  }
}
